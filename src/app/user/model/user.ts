export interface UserModel {
  id: string;
  userId: string; // Adicione userId
  access: string;
  imageUrl: string;
  firstName: string;
  lastName: string;
  email: string;
  password: string;
  upload: string; // Adicione upload
}
